<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <%- include ./partials/require %>

    <title>Edit Article</title>
</head>

<body>
    <%- include ./partials/header %>

    <div class="container">
        <%- include ./partials/error %>
        <%- messages('./partials/message', locals) %>
        <% if(typeof(article) != 'undefined' && article != '' && article) { %>
            
        <form action="/api/article/edit/<%= article._id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group row">
                <label for="inputEmail3" class="col-sm-2 col-form-label">Title</label>
                <div class="col">
                    <input type="text" class="form-control" name="title" placeholder="Title" value="<%= article.title %>">
                </div>
            </div>
            <div class="form-group row">
                <label for="inputEmail3" class="col-sm-2 col-form-label">Content</label>
                <div class="col">
                    <textarea type="text" class="form-control" name="content" placeholder="Content"><%= article.content %></textarea>
                </div>
            </div>
            <div class="form-group row">
                <label for="inputEmail3" class="col-sm-2 col-form-label">Picture</label>
                <div class="col">
                    <input type="file" class="form-control" name="picture">
                </div>
            </div>
            <img src="/images/<%= article.picture %>" alt="" width="300" height="200">
            <br><br>
                
            <div class="form-group row">
                <div class="col">
                    <button type="submit" class="btn btn-success">Save :)</button>
                    or just close the page to cancel
                </div>
            </div>
        </form>
        <% }%>
    </div>
    <%- include ./partials/footer %>
</body>

</html>